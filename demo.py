"""Entry point. The retrieval pipeline is added branch by branch; see README."""

if __name__ == "__main__":
    print("Nothing to run yet: ingestion, retrieval, routing and ablation land in later branches.")
