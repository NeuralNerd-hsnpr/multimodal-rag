-r requirements.txt
# Only scripts/caption.py needs transformers directly; sentence-transformers pulls it in transitively for the demo.
transformers==5.16.1
pytest==8.4.2
matplotlib==3.11.1
reportlab==5.0.1
